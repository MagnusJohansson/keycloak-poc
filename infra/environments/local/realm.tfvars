# Local Docker Keycloak (`make up`).
# Admin username/password against the master realm via the built-in admin-cli.
keycloak_url    = "http://localhost:8080"
admin_client_id = "admin-cli"
admin_username  = "admin"
admin_password  = "admin"

web_react_origin = "http://localhost:5173"
web_vue_origin   = "http://localhost:5174"
api_origin       = "http://localhost:5001"

smtp_host = "mailpit"
smtp_port = "1025"
