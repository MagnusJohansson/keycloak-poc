<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="docvault_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!--
          Flutter's template sets android:taskAffinity="" on MainActivity. It is
          deliberately NOT set here.

          An empty affinity means "no affinity to any task", so the activity starts
          its own. AppAuth's RedirectUriReceiverActivity and
          AuthorizationManagementActivity come from the library manifest and use the
          default package-name affinity, so with "" they end up in THREE separate
          tasks. The browser redirect then cannot resume the singleTask
          AuthorizationManagementActivity holding the pending request, AppAuth logs

            W/AppAuth: No stored state - unable to handle response

          and authorizeAndExchangeCode never completes - silently, with no exception.

          Setting "" on AppAuth's activities as well does NOT fix it: an empty
          affinity is not a SHARED affinity, so each still gets its own task.
          Confirmed on an emulator with `dumpsys activity activities` - tasks #58,
          #59 and #60 for the three activities.

          Trade-off: the empty affinity was hardening against task-affinity
          hijacking. To keep both, give MainActivity and the two AppAuth activities
          the same explicit non-default affinity instead of removing it.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            tools:node="merge" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
