<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="docvault_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--
          Put AppAuth's activities in the SAME task as MainActivity.

          Flutter's template sets android:taskAffinity="" on MainActivity (hardening
          against task-affinity hijacking), but AppAuth's activities come from the
          library manifest and inherit the default affinity - the package name. They
          therefore land in a DIFFERENT task, and because
          AuthorizationManagementActivity is launchMode="singleTask", the browser
          redirect creates a fresh instance instead of resuming the one holding the
          pending authorization request. AppAuth then logs:

            W/AppAuth: No stored state - unable to handle response

          and authorizeAndExchangeCode never returns - no error, no exception, the
          Future simply never completes. Aligning the affinities fixes it while
          keeping MainActivity's hardening.
        -->
        <activity
            android:name="net.openid.appauth.AuthorizationManagementActivity"
            android:taskAffinity=""
            tools:node="merge" />
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:taskAffinity=""
            tools:node="merge" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
