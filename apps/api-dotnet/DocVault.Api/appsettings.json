{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "DocVault.Auth": "Information"
    }
  },
  "AllowedHosts": "*",
  "Keycloak": {
    "_comment": "Authority is the realm issuer. Swap this one value to move between the local Docker Keycloak and an Azure deployment - nothing else in the API changes.",
    "Authority": "http://localhost:8080/realms/docvault",
    "Audience": "docvault-api",
    "AnalyticsRedirectUri": "http://localhost:5001/analytics/callback",
    "RequireHttpsMetadata": false,
    "_comment2": "RequireHttpsMetadata is false only because Authority is loopback (the local Docker lab). The API refuses to start if this is false for a non-loopback authority."
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5173",
      "http://localhost:5174"
    ]
  }
}