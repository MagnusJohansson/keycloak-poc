{
  "Logging": { "LogLevel": { "Default": "Information" } },
  "Keycloak": {
    "Authority": "http://localhost:8080/realms/docvault",
    "ClientId": "docvault-worker",
    "_comment": "ClientSecret is intentionally absent. Supply it via `make show-secrets` -> user-secrets or environment, and via Key Vault in production. Never commit it.",
    "ClientSecret": "",
    "ApiBaseUrl": "http://localhost:5001"
  }
}
